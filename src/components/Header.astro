---
export interface Props {
    title: string;
}
const { title } = Astro.props;
---
<div>
    <!-- Header section -->
    <header class="site-header">
        <h1 class="site-title">{title}</h1>
    </header>
    <hr class="divider" />
</div>

